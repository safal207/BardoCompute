name: Continuous Missed-Work Factorial v0.26

on:
  push:
    branches:
      - research/v0.26-continuous-missed-work
    paths:
      - benchmarks/continuous_missed_work_factorial_v026.py
      - .github/workflows/continuous-missed-work-v026.yml
  workflow_dispatch:

permissions:
  contents: read
  issues: write

jobs:
  benchmark:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - run: python -m pip install --upgrade pip
      - run: pip install -e . pytest
      - run: pytest -q tests/test_exchange.py
      - name: Run Continuous Missed-Work Factorial v0.26
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python benchmarks/continuous_missed_work_factorial_v026.py | tee /tmp/v026.log
          awk '/^\[continuous_factorial\]$/{show=1} show{print}' /tmp/v026.log > /tmp/causal.txt
          {
            echo "Hosted v0.26 — Python ${{ matrix.python-version }}"
            echo
            echo '```text'
            cat /tmp/causal.txt
            echo '```'
            echo
            echo "commit: $GITHUB_SHA"
          } > /tmp/comment.md
          gh issue comment 12 --repo "$GITHUB_REPOSITORY" --body-file /tmp/comment.md

# Trigger-only workflow metadata change; benchmark/prereg semantics unchanged.
